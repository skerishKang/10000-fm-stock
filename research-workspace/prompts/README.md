# Prompts Workspace

Reusable prompts for converting manually gathered source references into structured candidates.

Prompt outputs are candidates only. Human review is required before promotion into `data/*.json`.
